sudo: false
language: php
cache:
  directories:
    - $HOME/.composer/cache
php:
  - 7.0
  - 5.6
install:
  - composer self-update
  - composer install --prefer-dist
- ./vendor/bin/phpunit --bootstrap tests/bootstrap.php --configuration phpunit.xml tests